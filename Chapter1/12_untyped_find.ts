namespace UntypedFind {
    // hard to tell what kind of arguments it expects
    // We need to read the implementation, pass in our best guess, 
    // and see whether we get a run-time error or hope that the documentation covers this.
    declare function find(range: any, pred: any): any;
}